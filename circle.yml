machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - npm install -g gulp
    - bower install

test:
  override:
    - echo 'test'

deployment:
  production:
    branch: master
    commands:
      - git config --global user.email "ide.yuta@gmail.com"
      - git config --global user.name "Yuta Ide"
      - gulp build
      - gulp deploy
