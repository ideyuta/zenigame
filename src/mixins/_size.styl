size($width, $height = $width)
  width: $width
  height: $height
minsize($width, $height = $width)
  min-width: $width
  min-height: $height
maxsize($width, $height = $width)
  max-width: $width
  max-height: $height
